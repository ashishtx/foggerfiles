tables:
    emp_details:
        columns:
            emp_id: { maskStrategy: hashify, options: {  } }
            empname: { maskStrategy: none, options: {  } }
        subsetStrategy: null
        subsetOptions: {  }
excludes: {  }
